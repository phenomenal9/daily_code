import java.util.*;
import java.lang.Math;
public class Main
{
    public static int subset_problem(int[] arr)
    {
        int a = 1;
        for(int i=0; i<arr.length; i++)
        {
            a = a*Math.pow(arr[i],i+2);
        }
        return a;
    }
	
}
